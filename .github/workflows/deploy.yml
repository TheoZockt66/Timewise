name: Deploy

on:
  workflow_dispatch:

jobs:
  build-and-test:
    uses: ./.github/workflows/webpack.yml
  
  deploy:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
      - name: Send HTTP POST
        run: |
          curl -X POST "https://api.vercel.com/v1/integrations/deploy/prj_5b9LqgP0wJXdZiwkXAYYk5Th0FXa/4e1Es26Gae" \
            -H "Content-Type: application/json" \'

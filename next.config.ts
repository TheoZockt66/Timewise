import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Aktiviert React Strict Mode für bessere Fehlererkennung in der Entwicklung
  reactStrictMode: true,
};

export default nextConfig;
